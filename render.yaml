services:
  - type: web
    name: national-rail-command-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
      - key: GROQ_API_KEY
        sync: false # Requires manual entry in the Render dashboard
